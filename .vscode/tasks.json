{
    "version": "2.0.0",
    "tasks": [
    {
        "label": "Run",
        "type": "shell",
        "command": "cmd",
        "args": ["/c", "run.bat"],
        "options": {
            "cwd": "${workspaceFolder}"
        },
        "group": {
            "kind": "build",
            "isDefault": true
        }
    }
    ]
}